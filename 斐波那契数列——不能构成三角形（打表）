#include <stdio.h>
int main()
{
	int t,n,k=0,i;
	int a[6]={1,2,3,5,8,13};
	while(scanf("%d",&t)!=EOF)
	{
		while(t--)
		{
			scanf("%d",&n);
			for(i=0;i<6;i++)
			{
				if(n<a[i])
				{
					break;
				}
			
			}
			printf("Case #%d: %d\n",++k,n-i);
		}
	
		
	}
	return 0;
	
}
